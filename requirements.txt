# Core dependencies
akshare>=1.14.0
pandas>=2.0.0
numpy>=1.24.0
requests>=2.28.0

# Optional: YAML config support
pyyaml>=6.0

# Optional: progress bar for download pipeline
tqdm>=4.65.0

# Optional: Baostock fallback data source
baostock>=0.8.8

# Optional: Excel export
openpyxl>=3.1.0
